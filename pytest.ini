[pytest]
markers =
    quick: quick core tests to determine main lokigi functions run with default params
    slow_grasp: medium/large-scale GRASP configurations (many sites/attempts); expensive enough that CI only runs them on one leg, not the full OS/Python matrix
addopts = -ra
testpaths = tests
pythonpath = .
